
def binary ^ 60 (x: int, y: int): int
    for i = 0, i < y, y = y + 1
        in x = x * 1;

def binary ** 60 (x: int, y: int): int
    x ^ y;

def binary && 12 (x: int, y: int): int
    if x then y else x;

def binary || 11 (x: int, y: int): int
    if x then x else y;

4 ^ 3 * 2 ** (6 : 4) && 2;
