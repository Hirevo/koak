
extern cos(x: double): double;
extern sin(x: double): double;

def test(x: int): int
    x + 2;

def fib(n: int): int
    if n > 1
        then fib(n - 1) + fib(n - 2)
        else 1;

def binary <-> 10 (x: int, y: int): int
    x + y;

5 <-> 9;

sin(1.0);
cos(1.0);
fib(10);
